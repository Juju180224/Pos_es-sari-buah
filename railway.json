{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "sh -c 'php artisan storage:link --force && php artisan optimize:clear && php artisan serve --host=0.0.0.0 --port=$PORT'"
  }
}